# إعداد Supabase - خطوتين فقط!

## ✅ المشروع الآن يستخدم Supabase فقط!

تم تحديث المشروع بالكامل ليستخدم Supabase API بدلاً من قاعدة البيانات المحلية.

---

## الخطوة 1️⃣: إنشاء الجداول في Supabase

1. اذهب إلى: https://app.supabase.com
2. اختر مشروعك
3. من القائمة اليسرى، ابحث عن **SQL Editor** (عادة تحت قسم Tools أو Platform)
4. اضغط **New Query**
5. افتح ملف `create_tables_supabase.sql` من المشروع
6. انسخ **كل** محتواه والصقه في SQL Editor
7. اضغط **Run** (أو Ctrl+Enter)

**النتيجة المتوقعة:**
```
total_users
-----------
0
```

هذا يعني تم إنشاء الجداول بنجاح!

---

## الخطوة 2️⃣: استيراد المستخدمين (101 حساب)

لا تزال في **SQL Editor**:

1. اضغط **New Query** مرة أخرى
2. افتح ملف `import_users_READY.sql`
3. انسخ **كل** محتواه (الملف كبير - 16KB)
4. الصقه في SQL Editor
5. اضغط **Run**

**انتظر 10-20 ثانية...**

**النتيجة المتوقعة:**
```
total_users
-----------
101

Users imported successfully!
```

---

## الخطوة 3️⃣: شغل التطبيق

```bash
uvicorn main:app --reload
```

**إذا رأيت:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000
INFO:     Application startup complete.
```

**تم بنجاح! 🎉**

---

## الخطوة 4️⃣: افتح المتصفح

```
http://localhost:8000
```

**تسجيل الدخول:**
- Username: `admin`
- Password: `mustafaRaed1998$$`

---

## ما الذي تغير؟

✅ **تمت إزالة:**
- SQLAlchemy
- PostgreSQL connection string
- DATABASE_URL
- جميع قواعد البيانات المحلية

✅ **الآن يستخدم:**
- Supabase Python Client
- SUPABASE_URL (موجود في .env)
- SUPABASE_ANON_KEY (موجود في .env)

---

## لو فيه مشكلة؟

### خطأ: "SUPABASE_URL or SUPABASE_ANON_KEY missing"
✅ **الحل:** تأكد من وجود القيم في ملف `.env` - **وهي موجودة بالفعل!**

### خطأ: "relation 'users' does not exist"
✅ **الحل:** شغل ملف `create_tables_supabase.sql` في Supabase SQL Editor

### التطبيق يعمل لكن لا يمكنك تسجيل الدخول
✅ **الحل:** شغل ملف `import_users_READY.sql` لاستيراد الحسابات

---

## الملفات المهمة:

| الملف | الوصف |
|------|-------|
| `create_tables_supabase.sql` | إنشاء الجداول في Supabase |
| `import_users_READY.sql` | استيراد 101 مستخدم |
| `.env` | يحتوي على SUPABASE_URL و SUPABASE_ANON_KEY |

---

**🚀 الآن المشروع يعمل بالكامل على Supabase!**
