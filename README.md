# Alrt AI - Instagram Tracking System

نظام متابعة حسابات Instagram مع تحليلات متقدمة - يعمل بالكامل على Supabase.

## التثبيت السريع

### 1. تثبيت المكتبات

```bash
pip install -r requirements.txt
```

### 2. إعداد Supabase

**اقرأ ملف: `SUPABASE_SIMPLE_SETUP.md`** للخطوات المفصلة.

خطوات مختصرة:
1. شغل `create_tables_supabase.sql` في Supabase SQL Editor
2. شغل `import_users_READY.sql` في Supabase SQL Editor

### 3. تشغيل التطبيق

```bash
uvicorn main:app --reload
```

افتح: http://localhost:8000

---

## تسجيل الدخول




---

## الملفات المهمة

| الملف | الوصف |
|------|-------|
| `SUPABASE_SIMPLE_SETUP.md` | دليل إعداد Supabase (ابدأ هنا!) |
| `create_tables_supabase.sql` | إنشاء الجداول في Supabase |
| `import_users_READY.sql` | استيراد 101 مستخدم |
| `.env` | إعدادات البيئة (يحتوي على Supabase URL و Key) |
| `accounts.csv` | كلمات مرور المستخدمين (user001-user100) |

---

## المميزات

- ✅ متابعة حسابات Instagram
- ✅ تحليل نشاط الحسابات
- ✅ فحص إعلانات Facebook
- ✅ 10 عمال متوازيين (10x سرعة)
- ✅ نظام CRM مدمج
- ✅ قاعدة بيانات Supabase سحابية
- ✅ لا حاجة لقواعد بيانات محلية!

---

## التقنيات المستخدمة

- **Backend:** FastAPI
- **Database:** Supabase (PostgreSQL السحابي)
- **Scraping:** Apify
- **Authentication:** JWT + bcrypt
- **Scheduling:** APScheduler
- **Workers:** 10 parallel Instagram workers

---

## الأداء

| العملية | قبل (عامل واحد) | بعد (10 عمال) |
|---------|-----------------|---------------|
| إضافة 10 حسابات | 50 ثانية | ~7 ثوانٍ |
| إضافة 50 حساب | 4+ دقائق | ~35 ثانية |
| تحديث 100 حساب | 8+ دقائق | ~70 ثانية |

**تحسين السرعة: 7x إلى 10x أسرع!** ⚡

---

## API Endpoints

### المصادقة
- `POST /token` - تسجيل الدخول
- `POST /register` - تسجيل مستخدم جديد

### إدارة الحسابات
- `GET /targets` - عرض جميع الحسابات
- `POST /add_target` - إضافة حساب واحد
- `POST /bulk_add_targets` - إضافة عدة حسابات من CSV
- `DELETE /remove_target/{id}` - حذف حساب
- `POST /refresh_target/{id}` - تحديث حساب يدوياً
- `PUT /update_client_crm` - تحديث ملاحظات/تصنيفات
- `POST /check_facebook_ads/{id}` - فحص إعلانات فيسبوك

### المراقبة
- `GET /queue_status` - حالة الطابور
- `GET /data/dashboard` - بيانات لوحة التحكم

---

## استكشاف الأخطاء

**خطأ: "SUPABASE_URL or SUPABASE_ANON_KEY missing"**
- ✅ تأكد من وجود القيم في `.env` (موجودة بالفعل)

**خطأ: "relation 'users' does not exist"**
- ✅ شغل `create_tables_supabase.sql` في Supabase SQL Editor

**لا يمكنك تسجيل الدخول؟**
- ✅ شغل `import_users_READY.sql` لاستيراد الحسابات

---

## البنية

```
├── main.py                      # التطبيق الرئيسي
├── scheduler.py                 # المجدول مع 10 عمال
├── scraper.py                   # Apify scraper
├── database.py                  # Supabase client
├── requirements.txt             # المكتبات المطلوبة
├── .env                         # المتغيرات البيئية
├── create_tables_supabase.sql   # إنشاء الجداول
├── import_users_READY.sql       # استيراد المستخدمين
└── SUPABASE_SIMPLE_SETUP.md     # دليل الإعداد
```

---

**🚀 المشروع جاهز للعمل بالكامل على Supabase!**

**للبدء:** اقرأ `SUPABASE_SIMPLE_SETUP.md`
